#4.2 Discussion on Function-Call Stack

#If you keep pushing onto a stack without popping (often caused by infinite #recursion), the memory allocated for the stack runs out. This results in a #Stack Overflow error, and the program crashes.
